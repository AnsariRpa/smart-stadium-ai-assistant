# Use lightweight Node image
FROM node:18-alpine

WORKDIR /app

# Copy files
COPY package*.json ./
RUN npm install

COPY . .

# Cloud Run uses PORT env
ENV PORT=8080

EXPOSE 8080

# Start your app
CMD ["node", "backend/server.js"]